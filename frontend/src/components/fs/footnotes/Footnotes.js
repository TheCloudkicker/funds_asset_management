import React from 'react'
import { TableDetailContainer } from '../../../assets/styled-components/Containers'

const Footnotes = ({ dispatch, isDark }) => {
    return (
        <TableDetailContainer>

        </TableDetailContainer>
    )
}

export default Footnotes
